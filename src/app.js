/*

This is the import controller
but just use import(SASS) or require(JS/TS) inside the folder
to the main one
*/

//CSS STUFFF
require('./scss/main.scss')
//JAVASCRIPT STUFFFFF
alert(require('./js/sample.js'));
require('./js/sample.ts');
